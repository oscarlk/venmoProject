# URL of the Flask backend. Leave unset for local dev (defaults to
# http://localhost:8000). In production set this to your backend URL, e.g.
# VITE_API_URL=https://venmo-backend.onrender.com
VITE_API_URL=
